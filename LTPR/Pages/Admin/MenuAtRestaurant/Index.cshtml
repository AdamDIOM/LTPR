@page
@model LTPR.Pages.Admin.MenuAtRestaurant.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tblMenuAtRestaurant[0].RID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tblMenuAtRestaurant[0].MID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.tblMenuAtRestaurant) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.KID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.KID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.KID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

<div>
    <a asp-page="../Index">Back to List</a>
</div>