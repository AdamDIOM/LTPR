@page
@model LTPR.Pages.Admin.IngredientInItem.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tblIngredientInItem[0].MIID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tblIngredientInItem[0].IID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.tblIngredientInItem) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MIID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.KID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.KID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.KID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

<div>
    <a asp-page="../Index">Back to List</a>
</div>