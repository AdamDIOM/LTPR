@page
@model LTPR.Pages.Menus.IndexModel

@{
    ViewData["Title"] = "Menus";
}

<h1>Menus</h1>

<p>
    @*<a asp-page="Create">Create New</a>*@
</p>
<table class="table">
@foreach (var item in Model.tblMenus) {
        <tr>
            <td>

                <a asp-page="./Details" asp-route-id="@item.ID">@Html.DisplayFor(modelItem => item.Name)</a>@*|<a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> 
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>*@
            </td>
        </tr>
}
</table>
